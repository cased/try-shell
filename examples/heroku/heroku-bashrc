# Create and enter a temporary directory
dir=$HOME/$$
mkdir -p $dir
cd $dir

# Clean it up when we're done
trap "rm -rf $dir" EXIT

# Tell all other programs (like the Heroku CLI) that this temp directory is our home
export HOME=$dir

yes | heroku login